diff --git a/src/api/pulls.ts b/src/api/pulls.ts
--- a/src/api/pulls.ts
+++ b/src/api/pulls.ts
@@ -5,7 +5,6 @@ import { z } from 'zod';
 export const PullSummary = z.object({
   id: z.string(),
   number: z.number().int(),
   title: z.string(),
-  deletedAt: z.string().nullable(),
   status: z.enum(['open', 'merged', 'closed']),
 });

@@ -19,7 +18,6 @@ export async function listPulls(repoId: string): Promise<PullSummary[]> {
       id: row.id,
       number: row.number,
       title: row.title,
-      deletedAt: row.deletedAt,
       status: row.status,
     };
   });
