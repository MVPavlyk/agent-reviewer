diff --git a/src/api/pulls.ts b/src/api/pulls.ts
--- a/src/api/pulls.ts
+++ b/src/api/pulls.ts
@@ -6,6 +6,7 @@ export const PullSummary = z.object({
   id: z.string(),
   number: z.number().int(),
   title: z.string(),
+  labels: z.array(z.string()).optional(),
   status: z.enum(['open', 'merged', 'closed']),
 });

@@ -18,6 +19,7 @@ export async function listPulls(repoId: string): Promise<PullSummary[]> {
       id: row.id,
       number: row.number,
       title: row.title,
+      labels: row.labels ?? [],
       status: row.status,
     };
   });
diff --git a/src/api/pulls.test.ts b/src/api/pulls.test.ts
--- a/src/api/pulls.test.ts
+++ b/src/api/pulls.test.ts
@@ -12,4 +12,9 @@ describe('listPulls', () => {
     const rows = await listPulls('repo-1');
     expect(rows[0].title).toBe('Add rate limiting');
   });
+
+  it('includes labels when present', async () => {
+    const rows = await listPulls('repo-1');
+    expect(rows[0].labels).toEqual(['needs-review']);
+  });
 });
