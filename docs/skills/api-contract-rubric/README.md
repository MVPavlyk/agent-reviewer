# API Contract Rubric — import fixture

Human-demonstration fixture for docs/specs/skills.md PR 3/8/9. `SKILL.md` is
the skill body; `install.sh` is a **decoy** — it must appear under
`ignored_entries` in the import preview, never be read, and never run. Zipped
as `api-contract-rubric.zip` for the manual walkthrough
(`./scripts/dev.sh` → `/skills` → Add → Import).
